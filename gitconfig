[user]
	name = ThiagoNod
	email = thiagoints@gmail.com
[credential]
	helper = store
[alias]
    push-github = !git checkout master && git pull && git checkout dev && git rebase master && git checkout master && git merge dev && git push
    gits = !git status -uno
[core]
	editor = vim
